# 2D 横版视觉自动控制系统：由浅入深开发路线

> 适用范围：用于自有、离线或明确允许自动化的测试环境。  
> 当前既定硬件：Raspberry Pi Pico 2 W。  
> 当前推荐总体思路：**位置巡逻（Patrol Routine） + 局部攻击检测（Local Attack Detection） + 卡住恢复（Stuck Recovery） + Pico 2 W USB HID 执行**。  
> 当前阶段不考虑复杂地图、A*、多层平台、梯子、传送点、复杂怪物分类，也不做反作弊规避。

---

## 1. 最终目标

目标系统：

```text
视频采集卡 / 外置摄像头
          │
          ▼
        A 电脑
  Capture / OpenCV
          │
          ├── Player Localization
          ├── Local Attack Detection
          ├── Patrol Controller
          ├── Stuck Detection
          └── State Machine
          │
        Wi-Fi
          ▼
   Raspberry Pi Pico 2 W
          │
      USB HID
          ▼
        B 电脑
   Keyboard / Mouse
```

第一版只需要做到：

```text
能够稳定获取画面
→ 能知道玩家大致位置
→ 能控制角色向左/向右
→ 能在地图两个端点之间往返
→ 卡住时会跳
→ 局部区域出现目标时执行攻击
```

---

## 2. 开发原则

- 一次只增加一个新变量。
- 每个阶段必须有明确验收条件。
- 所有视觉模块先离线测试，再接实时输入。
- 先“观察决策”，再“真正控制”。
- 当前阶段不引入 YOLO、A*、强化学习等复杂模块。

---

## 3. Phase 0 — 建立项目骨架

### 目标

建立统一代码结构、配置文件和日志机制。

```text
game_control/
│
├── README.md
├── requirements.txt
├── config.yaml
│
├── pc/
│   ├── main.py
│   ├── capture.py
│   ├── perception.py
│   ├── player_locator.py
│   ├── attack_detector.py
│   ├── controller.py
│   ├── state_machine.py
│   ├── pico_client.py
│   └── logger.py
│
├── pico/
│   ├── CMakeLists.txt
│   ├── main.c
│   ├── wifi.c
│   ├── hid_keyboard.c
│   ├── hid_mouse.c
│   └── protocol.c
│
├── config/
│   ├── map_01.yaml
│   └── templates/
│
├── samples/
│   ├── videos/
│   └── images/
│
└── tests/
```

Python 第一阶段只需要：

```text
opencv-python
numpy
pyyaml
```

### 验收

```text
[ ] Git 仓库建立
[ ] Python 环境能够运行
[ ] config.yaml 可以读取
[ ] 日志可以写入 logs/
```

全部通过才进入 Phase 1。

---

## 4. Phase 1 — Pico 2 W 单独模拟键盘

### 目标

完全不考虑 Wi-Fi、视觉和游戏。

只确认：

> Pico 2 W 能否通过 USB 被测试电脑识别为 HID Keyboard。

连接：

```text
Pico 2 W
   │
Micro-USB DATA
   │
   ▼
测试电脑
```

推荐使用：

```text
Pico SDK
+
TinyUSB
```

第一版实现：

```text
KEY_DOWN
KEY_UP
KEY_PRESS
RELEASE_ALL
```

### 测试

打开文本编辑器，测试单次按键、按住、释放和方向键。

### 验收

```text
[ ] 系统正确枚举 HID Keyboard
[ ] 单次按键正常
[ ] 按住正常
[ ] 松开正常
[ ] RELEASE_ALL 后无键卡住
[ ] 连续执行 100 次无异常
```

---

## 5. Phase 2 — Pico 同时模拟键盘和鼠标

### 目标

验证 USB Composite HID。

```text
Pico 2 W
   │
   ▼
USB Composite Device
├── Keyboard
└── Mouse
```

实现：

```text
KEY_PRESS
KEY_DOWN
KEY_UP
MOUSE_MOVE
LEFT_CLICK
RIGHT_CLICK
```

### 验收

```text
[ ] Keyboard 和 Mouse 可同时存在
[ ] 鼠标移动正常
[ ] 左右键正常
[ ] 键盘与鼠标互不干扰
[ ] 运行 10 分钟无 USB 断连
```

---

## 6. Phase 3 — A 电脑通过 Wi-Fi 控制 Pico

### 目标

暂时完全不使用视觉。

```text
A电脑
  │
Wi-Fi
  ▼
Pico 2 W
  │
USB HID
  ▼
测试电脑
```

第一版协议保持极简：

```text
PING
KEY_DOWN LEFT
KEY_UP LEFT
KEY_PRESS X 50
MOUSE_MOVE 20 0
LEFT_CLICK
RELEASE_ALL
```

A 电脑先做 CLI：

```text
> ping
PONG

> press x
OK
```

### 安全机制

Pico 必须实现：

```text
heartbeat timeout
        ↓
RELEASE_ALL
```

### 验收

```text
[ ] A → Pico 通信稳定
[ ] Pico 能解析命令
[ ] 连续发送 1000 条命令无明显异常
[ ] A 断线后 Pico 自动 RELEASE_ALL
[ ] Pico 重连后恢复正常
```

---

## 7. Phase 4 — 视频输入

### 目标

只解决：

> A 电脑能否稳定获得游戏画面。

优先使用视频采集卡。

```text
B电脑 HDMI
   │
   ▼
Capture Card
   │ USB
   ▼
A电脑
```

第一版程序只做：

```text
读取视频
显示画面
显示实际 FPS
保存截图
按 Q 退出
```

记录：

```text
width
height
FPS
frame timestamp
```

### 验收

连续运行至少 30 分钟：

```text
[ ] 无明显掉帧
[ ] 无设备断连
[ ] 分辨率固定
[ ] FPS 稳定
[ ] 能保存截图
[ ] 延迟可接受
```

---

## 8. Phase 5 — 建立离线测试数据

### 目标

不要立刻写实时识别。

先录制测试视频，覆盖：

```text
正常左右移动
没有目标
有目标
目标进入攻击区域
攻击动画
玩家跳跃
角色卡在障碍附近
地图滚动
```

建议：

```text
5~10 分钟视频
+
50~100 张关键截图
```

### 验收

```text
[ ] 有真实采集数据
[ ] 包含正常情况
[ ] 包含异常情况
[ ] 可以离线逐帧读取
```

---

## 9. Phase 6 — 玩家定位

### 目标

只解决一个问题：

> 玩家现在在哪里？

如果游戏有稳定 minimap，优先从 minimap 定位玩家。

输出统一成：

```python
PlayerState(
    visible=True,
    x=520,
    y=610,
    confidence=0.93
)
```

### 离线测试

对录制视频逐帧运行并保存可视化结果。

### 验收

```text
[ ] 正常场景玩家检测成功率 > 95%
[ ] 连续运动时坐标没有大量跳变
[ ] 不会经常把其他 UI 当成玩家
[ ] 玩家丢失时返回 visible=False
```

---

## 10. Phase 7 — 只做“决策显示”，不操作游戏

### 目标

构建第一版 Patrol Controller。

先把地图抽象成：

```text
LEFT_END ---------------- RIGHT_END
```

例如：

```yaml
left_x: 100
right_x: 900
tolerance: 30
```

Controller 输出：

```text
MOVE_LEFT
MOVE_RIGHT
STOP
```

但暂时不要发送给 Pico。

屏幕显示：

```text
Player X: 520
Target: RIGHT_END
Decision: MOVE_RIGHT
```

### 验收

```text
[ ] 每一帧决策方向基本正确
[ ] 到右端会切换为 LEFT
[ ] 到左端会切换为 RIGHT
[ ] 坐标轻微抖动不会频繁换方向
```

---

## 11. Phase 8 — 第一次闭环：只有左右移动

### 目标

把 Phase 7 的决策真正发送到 Pico。

当前只允许：

```text
LEFT
RIGHT
STOP
```

暂时禁止：

```text
ATTACK
JUMP
```

结构：

```text
Capture
  ↓
Player Localization
  ↓
Patrol Controller
  ↓
Pico
  ↓
LEFT / RIGHT
```

采用闭环更新，而不是一次性长时间按键。

### 验收

```text
[ ] 能从左向右移动
[ ] 到右端自动反向
[ ] 到左端再次反向
[ ] 连续往返 10 次
[ ] 不出现 LEFT + RIGHT 同时按下
[ ] 停止程序后所有键立即释放
```

---

## 12. Phase 9 — 卡住检测

### 目标

检测：

> 程序要求角色移动，但角色实际上没有移动。

记录最近 N 次 `player_x`。

如果：

```text
当前 action = MOVE_RIGHT
```

但：

```text
max(x_history) - min(x_history) < STUCK_THRESHOLD
```

则：

```text
stuck = True
```

第一版只报警，不自动跳。

### 验收

```text
[ ] 真正卡住能被识别
[ ] 正常缓慢移动不会误报
[ ] STOP 状态不会误报 stuck
```

---

## 13. Phase 10 — Jump Recovery

### 目标

加入第一个恢复动作：

```text
STUCK
↓
JUMP
↓
继续原移动方向
```

状态：

```text
PATROL
  │
  │ stuck
  ▼
RECOVER_JUMP
  │
  │ recovery timeout
  ▼
PATROL
```

必须设置：

```text
jump cooldown
recovery timeout
max retry
```

### 验收

```text
[ ] 多次简单障碍测试可恢复
[ ] 不会在正常路面频繁跳
[ ] 失败后不会无限重复 Jump
```

---

## 14. Phase 11 — Attack ROI 可视化

### 目标

现在才开始加入目标检测。

不做：

```text
追怪
怪物分类
全屏检测
```

只做：

> 玩家附近固定攻击区域里有没有目标。

```text
         Attack ROI
       ┌─────────────┐
玩家 → │             │
       │    target   │
       └─────────────┘
```

输出：

```python
AttackState(
    target_present=True,
    score=0.87
)
```

第一版推荐：

```text
Template Matching
```

必要时使用多模板。

### 验收

统计：

```text
True Positive
False Positive
False Negative
```

只有误报率可以接受以后才进入下一阶段。

---

## 15. Phase 12 — 时间滤波

### 目标

避免单帧误检测直接触发。

例如最近 5 帧：

```text
0 1 1 1 0
```

如果：

```text
>= 3 帧检测到
```

才认为：

```text
target_present = True
```

也可以设置进入/保持不同阈值。

### 验收

```text
[ ] 单帧噪声不会触发
[ ] 目标出现后响应足够快
[ ] 目标短暂遮挡不会立刻丢失
[ ] 目标消失后能及时恢复 False
```

---

## 16. Phase 13 — 攻击提示模式

### 目标

Controller 已经知道：

```text
Patrol Direction
+
Target Present
```

先只输出：

```text
MOVE_RIGHT
ATTACK
```

但仍然不真正执行攻击。

建立动作优先级，例如：

```text
RECOVER
>
ATTACK
>
PATROL
```

### 验收

```text
[ ] 应攻击时显示 ATTACK
[ ] 无目标时不会 ATTACK
[ ] Jump Recovery 时不会产生冲突命令
```

---

## 17. Phase 14 — 第一次完整闭环

### 目标

开启：

```text
Patrol
+
Jump Recovery
+
Attack
```

第一版状态机：

```text
                ┌──────────────┐
                │    PATROL    │
                └──────┬───────┘
                       │
          ┌────────────┴─────────────┐
          │                          │
     target found                  stuck
          │                          │
          ▼                          ▼
    ┌──────────┐              ┌──────────────┐
    │  ATTACK  │              │ RECOVER_JUMP │
    └────┬─────┘              └──────┬───────┘
         │                            │
 target gone                    recovered
         │                            │
         └──────────→ PATROL ←────────┘
```

### 验收

依次进行：

```text
5 分钟
15 分钟
30 分钟
```

观察：

```text
方向切换错误
卡住
误攻击
漏攻击
按键未释放
网络断连
视觉丢失
```

---

## 18. Phase 15 — 日志与可重复测试

### 目标

从“感觉能跑”升级为：

> 有数据证明稳定。

每个循环记录：

```text
timestamp
frame_id
player_x
player_visible
target_present
target_score
state
action
network_latency
stuck
```

例如：

```csv
timestamp,player_x,target,state,action
12:00:00.100,520,0,PATROL,MOVE_RIGHT
12:00:00.200,531,0,PATROL,MOVE_RIGHT
12:00:00.300,542,1,ATTACK,ATTACK
```

建议统计：

```text
Player localization success rate
Attack detection precision / recall
False positive rate
Stuck recovery success rate
Network command loss rate
End-to-end decision latency
```

---

## 19. Phase 16 — Robustness

只有前面全部稳定以后再做。

考虑：

```text
技能特效
亮度变化
不同窗口尺寸
视频采集掉帧
Wi-Fi 抖动
目标遮挡
玩家暂时不可见
程序异常退出
Pico 重连
```

必须定义 fail-safe：

```text
player_visible = False
        ↓
RELEASE_ALL

A disconnect
        ↓
Pico RELEASE_ALL

Controller exception
        ↓
RELEASE_ALL
```

---

## 20. 暂时不要做的内容

在 MVP 稳定之前，不建议做：

```text
YOLO
多类别怪物识别
怪物追踪
ByteTrack
复杂地图建模
Platform Graph
A*
强化学习
OCR
Web UI
数据库
Docker
动态路径规划
```

---

## 21. 推荐开发顺序总览

| 阶段 | 功能 | 是否控制目标电脑 | 通过标准 |
|---|---|---:|---|
| P0 | 项目结构 | 否 | 环境可运行 |
| P1 | Pico Keyboard HID | 测试电脑 | 稳定按键 |
| P2 | Keyboard + Mouse HID | 测试电脑 | Composite 稳定 |
| P3 | Wi-Fi → Pico | 测试电脑 | 命令稳定 |
| P4 | 视频采集 | 否 | 30 min 稳定 |
| P5 | 测试数据集 | 否 | 数据完整 |
| P6 | Player Localization | 否 | >95% 基础场景 |
| P7 | Patrol Decision | 否 | 决策正确 |
| P8 | 左右闭环移动 | 是 | 10 次往返 |
| P9 | Stuck Detection | 是 | 能稳定检测 |
| P10 | Jump Recovery | 是 | 简单障碍恢复 |
| P11 | Attack ROI | 否 | 检测可靠 |
| P12 | 时间滤波 | 否 | 降低误报 |
| P13 | Attack Decision | 否 | 决策正确 |
| P14 | 完整闭环 | 是 | 30 min 基础稳定 |
| P15 | 日志和指标 | 是 | 可量化 |
| P16 | 鲁棒性增强 | 是 | 异常可恢复 |

---

## 22. 现在拿到 Pico 后，第一轮只做什么

第一轮不要做目标检测。

先完成：

```text
1. Pico USB Keyboard
2. Pico USB Keyboard + Mouse
3. A PC → Wi-Fi → Pico
4. Capture Card / Camera → A PC
5. 录制测试数据
```

你首先需要打通两条完全独立的链路：

### 输出链路

```text
A电脑
发送 MOVE_RIGHT
        ↓
Wi-Fi
        ↓
Pico
        ↓
USB HID
        ↓
测试电脑收到 RIGHT
```

### 输入链路

```text
视频源
 ↓
A电脑
 ↓
OpenCV 能稳定读取
```

当这两条链路都独立稳定以后，再进入视觉和闭环控制。

---

## 23. AI Coding Agent 的使用方式

每次只给 AI 一个 Phase。

例如第一个任务：

```text
项目：Raspberry Pi Pico 2 W USB HID Controller

当前只实现 Phase 1。

目标：
让 Pico 2 W 通过 USB 被电脑识别为 HID Keyboard。

要求：
- 使用 Pico SDK
- 使用 TinyUSB
- 实现 KEY_DOWN / KEY_UP / KEY_PRESS / RELEASE_ALL
- 暂时不要加入 Wi-Fi
- 暂时不要加入鼠标
- 暂时不要加入视觉逻辑
- 提供构建和烧录说明

验收：
1. Windows 能识别 HID Keyboard
2. 可以向记事本输入 A
3. 可以按住和释放方向键
4. 连续测试 100 次无按键卡住

不要实现 Phase 2 及之后内容。
```

验收通过后，再把：

```text
Phase 1: PASS
```

告诉 AI，让它继续 Phase 2。

---

## 24. MVP 完成定义

当以下链路能够稳定工作：

```text
视频
 ↓
玩家定位
 ↓
左右巡逻
 ↓
到端点反向
 ↓
卡住检测
 ↓
自动跳跃恢复
 ↓
局部目标检测
 ↓
攻击
 ↓
继续巡逻
```

并且：

```text
连续运行 30 分钟
无严重卡死
无持续按键未释放
无频繁错误方向切换
网络断开能安全释放所有按键
```

则：

```text
MVP = COMPLETE
```

之后再讨论：

```text
复杂地图
多平台
目标追踪
更强视觉模型
动态路径规划
```
